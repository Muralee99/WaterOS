# WaterOS — Demo Video Script

**Target Duration:** 3 minutes 30 seconds
**Tone:** Confident, architectural, precise — no filler words
**Audience:** Google AI Hackathon judges (technical + product)

---

## 🎬 FULL SCRIPT

---

### [0:00 – 0:18] OPENING — The Problem

> *Screen: Black background → WaterOS logo fades in*

**NARRATION:**

"Water insecurity affects 4 billion people annually.
Floods, droughts, contamination, and aging infrastructure —
these aren't slow-moving problems.
They're real-time crises that demand real-time intelligence.

WaterOS is an AI-native water intelligence platform
built on Google ADK and Gemini 1.5 Pro —
deploying 14 autonomous agents that monitor, predict,
and respond to water threats across the globe."

---

### [0:18 – 0:42] DASHBOARD — The Nerve Center

> *Screen: Navigate to http://localhost:3000 → Dashboard loads*

**NARRATION:**

"This is the WaterOS command center.
At a glance — global water scores, reservoir levels,
active flood alerts, and a live sensor feed
updating every 5 seconds over WebSocket.

Every number you see here is backed by an agent.
Not a SQL query. Not a hard-coded value.
An autonomous reasoning system
that reads sensor data, applies domain intelligence,
and produces an actionable output."

> *Screen: Hover over a KPI card — show the source agent label*

"The platform covers 195 countries,
tracked at global, national, state, and city resolution —
a full geographic intelligence hierarchy."

---

### [0:42 – 1:08] COUNTRIES & AI INSIGHTS — Geographic Drill-Down

> *Screen: Click Countries in sidebar → CountriesPage loads*

**NARRATION:**

"Let's drill into country-level intelligence.
I'll select India."

> *Screen: Click on India row → AI Insights panel animates in above the table*

"Instantly — the AI Insights panel activates.
Water score 68 out of 100. Risk level: High.

Three insights generated by the Country Agent:
monsoon onset 2 weeks early, a 34% increase in flood probability in the Brahmaputra basin.
Groundwater depletion at 12 centimeters per year in the northwest.
Water stress index 0.71 — ranked 13th globally.

Two active alerts: red-level flood risk in 5 Assam districts,
and a conservation advisory in Rajasthan.

This isn't a dashboard with static text.
Every insight is produced by an agent
that read satellite data, sensor feeds,
and WHO benchmarks — seconds ago."

---

### [1:08 – 1:45] AGENT CONSOLE — The Core Innovation

> *Screen: Click Agent Console in sidebar*

**NARRATION:**

"This is where the architecture reveals itself —
the Agent Console."

> *Screen: Show the 14 agent cards in a grid*

"Fourteen specialized agents.
Flood prediction. Rainfall anomaly detection.
Reservoir optimization. Water quality analysis.
Leak detection. Climate modeling. Emergency coordination.

Each agent is built on Google's Agent Development Kit —
with a defined reasoning chain, tool registry,
and the ability to invoke other agents as sub-tasks.

I'll set a geographic context — India, Assam —
and run the Flood Agent."

> *Screen: Set Country=India, State=Assam in the dropdowns → click Run on Flood Agent*

"Watch the live trace."

> *Screen: Live trace steps animate in the agent card one by one*

"Each line you see appearing —
that's a reasoning step, streaming in real time.
The agent retrieved gauge station data,
applied a 50-year ML flood prediction model,
computed crest arrival time and at-risk districts.

This is explainable AI at the infrastructure level.
Not a black box.
A transparent chain of evidence."

> *Screen: Card expands showing full result — river level 5.2m, threshold 6.0m, flood risk MEDIUM*

"Brahmaputra at 5.2 meters.
Flood threshold at 6.0.
Storm probability 38% over the next 72 hours.
Recommendation: pre-position NDRF units.
No evacuation required at current levels.

An analyst would take hours to produce this.
The agent produced it in 3 seconds."

---

### [1:45 – 2:10] AGENT-TO-AGENT ORCHESTRATION — Decision Agent

> *Screen: Click Run on Decision Agent (with India/Assam context still set)*

**NARRATION:**

"Now I'll run the Decision Agent —
the master orchestrator.

Unlike the specialist agents,
the Decision Agent doesn't run alone.
It invokes the Emergency Agent,
the Rainfall Agent,
and the Reservoir Agent —
collecting their outputs as structured evidence.

This is Agent-to-Agent communication —
built on Google ADK's invocation chain protocol.
Agents calling agents, results flowing upward,
until the Decision Agent has enough evidence
to run a Multi-Criteria Decision Analysis."

> *Screen: Decision Agent card shows reasoning steps appearing — sub-agent invocations visible*

"You can see the invocations in the trace:
Emergency Agent — activated.
Rainfall Agent — activated.
Reservoir Agent — activated.

MCDA weights applied:
Water security 40%, economic impact 30%, environmental 30%.

Final output: prioritize pipe replacement in 3 critical sections.
Cost-benefit ratio: 4.2 to 1.
Confidence: 88%."

---

### [2:10 – 2:30] OBSERVABILITY — Sessions, Memory, Metrics

> *Screen: Click the Observability tab in the right panel*

**NARRATION:**

"Every agent run is fully observable.

The Observability panel shows real-time metrics:
total runs, average latency, success rate,
and a live execution trace for any agent currently running.

Below that — the session history.
Every execution saved to PostgreSQL
with a session ID, latency, confidence score,
tools called, and the complete reasoning chain."

> *Screen: Show the session history list with timestamps and tool counts*

"And agent memory.
After each successful run,
the platform writes an episodic memory record —
scope, confidence, key findings —
so the next time this agent runs for Assam,
it inherits context from what it already learned.

This is not stateless AI.
WaterOS agents remember."

---

### [2:30 – 2:50] DECISION AGENT CHAT — Natural Language Interface

> *Screen: Click Chat tab → type a question*

**NARRATION:**

"The Decision Agent also has a natural language interface.
I'll ask: 'What is the flood risk in Assam this week?'"

> *Screen: Type the question, hit Enter → response appears after ~1 second*

"The agent synthesizes sensor data, rainfall anomalies,
and reservoir levels into a concise situational report —
in under 2 seconds.

Chat history persists across page refreshes.
Backed by the same session tracking in PostgreSQL.
Every conversation is an audit record."

---

### [2:50 – 3:10] MCP SERVER — Standardized Tool Interface

> *Screen: Navigate to Workflow page or show API docs at localhost:8000/docs → /mcp/tools*

**NARRATION:**

"The platform exposes a Model Context Protocol server —
8 standardized tools that any agent can call
through a vendor-neutral interface.

Reservoir status. Flood prediction. Leak detection.
Water quality scoring. Rainfall forecasting.
Groundwater monitoring. Release optimization.
Emergency planning.

MCP decouples the agents from the data layer.
Swap the data source, the agent interface doesn't change.
That's the architectural contract that makes this scalable
beyond a hackathon — into production."

---

### [3:10 – 3:30] CLOSING — Stack & Impact

> *Screen: Architecture diagram from docs/architecture.md, or a clean stack slide*

**NARRATION:**

"WaterOS is built on:
Google ADK and Gemini 1.5 Pro at the intelligence layer.
FastAPI with async SQLAlchemy at the API layer.
PostgreSQL for agent sessions and memory.
Redis for real-time caching and pub/sub.
Apache Kafka for sensor event streaming.
Qdrant for vector search and semantic memory.
React 19 with a fully typed TypeScript frontend.

One Docker Compose command.
All services running.
Fully observable. Fully explainable.

Water insecurity is not a data problem.
Data exists.
It's an intelligence problem —
getting the right analysis to the right decision-maker
in seconds, not hours.

That is what WaterOS solves."

> *Screen: WaterOS logo + GitHub URL*

"WaterOS. Built for Google AI Hackathon 2025."

---

## 🎯 RECORDING CHECKLIST

### Before you record:
- [ ] Set `GOOGLE_API_KEY` in `.env` so Gemini responses are real
- [ ] Run `docker compose up -d` and wait for all services healthy
- [ ] Register an account at `http://localhost:3000/login`
- [ ] Pre-select India → Assam context in Agent Console
- [ ] Clear old agent results so the live trace is fresh
- [ ] Set browser to 1920×1080, zoom at 100%
- [ ] Close notifications, set Do Not Disturb

### Recording flow (in order):
1. Dashboard (KPIs, charts, WebSocket live data)
2. Countries → click India → AI Insights panel animates in
3. Agent Console → set India/Assam → Run Flood Agent → watch live trace
4. Expand result card → show reasoning chain + tools called
5. Run Decision Agent → show A2A invocations in trace
6. Observability tab → session history, stats, memory section
7. Chat tab → ask a question → show real Gemini response
8. API Docs at localhost:8000/docs → MCP tools endpoint
9. Closing slide

### Screen recording tools:
- **OBS Studio** (free, Windows/Mac/Linux) — best quality
- **Loom** — easy, auto-uploads
- **QuickTime** (Mac) — simplest

### Adding narration:
```bash
# Record voiceover separately with ElevenLabs or your own voice
# Merge with FFmpeg:
ffmpeg -i screen-recording.mp4 -i voiceover.mp3 \
  -c:v copy -c:a aac -shortest \
  wateros-demo-final.mp4
```

---

## ⏱ TIMING GUIDE

| Section | Start | End | Duration |
|---------|-------|-----|----------|
| Opening — The Problem | 0:00 | 0:18 | 18s |
| Dashboard | 0:18 | 0:42 | 24s |
| Countries + AI Insights | 0:42 | 1:08 | 26s |
| Agent Console + Live Trace | 1:08 | 1:45 | 37s |
| Decision Agent A2A | 1:45 | 2:10 | 25s |
| Observability Panel | 2:10 | 2:30 | 20s |
| Decision Agent Chat | 2:30 | 2:50 | 20s |
| MCP Server | 2:50 | 3:10 | 20s |
| Closing | 3:10 | 3:30 | 20s |
| **Total** | | | **3:30** |

---

## 💡 DELIVERY TIPS

- **Speak at 80% of your natural pace** — clarity over speed
- **Pause 1 second after clicking** — let the UI animate before speaking about it
- **Don't read the screen** — describe what it *means*, not what it *shows*
- **Confidence carries** — one slow, clear sentence beats three rushed ones
- **No "um", "basically", "kind of"** — cut them in editing if they slip in
- **Record in one pass if possible** — start-stop edits are audible
